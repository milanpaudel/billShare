<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body>
    <div class="container">
        <header>
            <h1>Welcome, {{username}}</h1>
            <nav>
                <a href="/dashboard"><i class="fas fa-home"></i> Dashboard</a>
                <a href="/groups/create"><i class="fas fa-users"></i> Create Group</a>
                <a href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </nav>
        </header>

        <main>
            <section>
                <h2>Your Groups</h2>
                <!-- You can loop over groups here if passed from backend -->
                {{#if groups.length}}
                <ul>
                    {{#each groups}}
                    <li><strong>{{name}}</strong> – {{members.length}} members</li>
                    {{/each}}
                </ul>
                {{else}}
                <p>No groups yet. <a href="/groups/create">Create one</a></p>
                {{/if}}
            </section>
        </main>
    </div>
</body>

</html>